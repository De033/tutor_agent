# -----------------------------------------------------------------------------
# API Configuration for OpenAI-Compatible Services
# -----------------------------------------------------------------------------
# This file serves as a template for your .env file.
# Copy this file to a new file named .env and fill in the values.
# The .env file should be kept private and not be committed to version control.

# Required: Your API key for the language model service.
OPENAI_COMPATIBLE_API_KEY=""

# Optional: The base URL of the API.
# If you are using a service other than the default, specify its endpoint here.
# For example, for a local model server: http://localhost:8080/v1
OPENAI_COMPATIBLE_API_BASE=""